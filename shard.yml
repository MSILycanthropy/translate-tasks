name: translate-tasks
version: 0.1.0

authors:
  - MSILycanthropy <ethanmichaelk@gmail.com>

targets:
  translate-tasks:
    main: src/translate-tasks.cr

crystal: 1.2.2

license: MIT

dependencies:
  terminal_table:
    github: benoist/terminal_table.cr
